# Terraform generated files
.terraform/                 # Terraform's working directory
.terraform.lock.hcl         # Dependency lock file for Terraform (can be committed, but sometimes ignored for local development consistency)
*.tfplan                    # Terraform plan files
*.tfstate                   # Terraform state file
*.tfstate.* # Terraform state backups
crash.log                   # Crash log files from Terraform
.terraformrc                # Local Terraform CLI configuration file (should be in ~/.terraformrc usually)
override.tf                 # Terraform override files (often for local testing)
override_*.tf               # Terraform override files
*.tfvars                    # Terraform variable definition files (sensitive values should be in .tfvars.json or loaded from secrets manager)
*.tfvars.json               # Terraform variable definition files (sensitive values)

# Terragrunt generated files
.terragrunt-cache/          # Terragrunt's working directory and cache
terragrunt.hcl.json         # Terragrunt's generated JSON config (not usually committed)
terragrunt-debug.tfvars     # Debug files generated by Terragrunt

# Provider-specific temporary files
.terraform-plugin-cache/    # Cache for Terraform provider plugins (from Terraform 0.14+)

# SSH Keys (generated by tls_private_key and local_file, or similar)
keys/                       # Directory where SSH private keys are saved (e.g., in modules/ec2/keys/)
*.pem                       # Private key files
*.key                       # Private key files

# IDE and OS generated files
.idea/                      # IntelliJ IDEA project files
.vscode/                    # VS Code settings
.DS_Store                   # macOS generated files
Thumbs.db                   # Windows generated files
*.swp                       # Vim swap files
*.bak                       # Backup files

# Sensitive files (if applicable, though best practice is to manage via secrets manager)
*.env                       # Environment variable files
.env.local
.env.development.local
.env.test.local
.env.production.local
*.secrets                   # Generic secrets files
secrets.tfvars              # Example for sensitive tfvars
secrets.json                # Example for sensitive JSON

# Cloud-specific temporary files (if any are left by tools)
*.kubeconfig                # Kubernetes config files (sensitive)

# Application specific build artifacts (if within the same repo)
dist/
build/
node_modules/